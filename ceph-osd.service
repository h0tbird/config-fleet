[Unit]
Description=Ceph OSD
After=docker.service
Requires=docker.service

[Service]
Restart=on-failure
RestartSec=20
TimeoutStartSec=0

ExecStartPre=-/usr/bin/docker kill ceph_osd
ExecStartPre=-/usr/bin/docker rm ceph_osd
ExecStartPre=-/usr/bin/docker pull regi01:5000/ceph/daemon:latest

ExecStart=/usr/bin/sh -c "/usr/bin/docker run \
  --privileged=true \
  --net=host \
  --name=ceph_osd \
  --volume /var/lib/ceph:/var/lib/ceph \
  --volume /etc/ceph:/etc/ceph \
  --volume /dev:/dev \
  --env CLUSTER='ceph' \
  --env CEPH_GET_ADMIN_KEY=1 \
  --env OSD_DEVICE=/dev/sda \
  --env OSD_FORCE_ZAP=1 \
  --env KV_TYPE=etcd \
  --env KV_IP=127.0.0.1 \
  --env KV_PORT=2379 \
  regi01:5000/ceph/daemon:latest osd"

ExecStop=/usr/bin/docker stop ceph_osd

[Install]
WantedBy=multi-user.target

[X-Fleet]
Global=true
MachineMetadata=role=slave
